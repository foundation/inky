[package]
name = "inky-ffi"
version = "2.0.0-beta.10"
edition = "2021"
description = "C FFI bindings for Inky"
license = "MIT"

[lib]
crate-type = ["cdylib", "staticlib"]
name = "inky"

[dependencies]
inky-core = { version = "2.0.0-beta.10", path = "../inky-core", features = ["css-inlining", "templating", "pipeline"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
cbindgen = "0.27"
